<style>
  body {
    overflow: hidden;
    width: 100%;
    height: 100%;
    background: #f3f3f3 !important;
  }
  #navbarNavAltMarkup a:hover {
    background-color: #383bff;
  }
  .activeMenu {
    background-color: #2729cf !important;
  }
  .none {
    display: none !important;
  }
  .container-form {
    max-height: 90vh;
  }
  .container-form form {
    max-height: 65vh;

    overflow-y: auto;
    -ms-overflow-style: none; /* IE and Edge */
    scrollbar-width: none;
    background-color: rgb(255, 255, 255);
  }
  .container-form form::-webkit-scrollbar {
    display: none;
  }
  .snake__bar {
    position: absolute;
    top: 50px;
    left: 100%;
    display: flex;
    padding: 0 16px;
    justify-content: flex-start;
    align-items: center;
    background: #fff;
    width: 250px;
    height: 48px;
    border-left: 3px solid #db2222;
    transition: all 0.5s ease;
    opacity: 0;
    box-shadow: rgba(100, 100, 111, 0.2) 0px 7px 29px 0px;
  }
  .showSnake__bar__error {
    border-left: 3px solid #db2222;
    transform: translateX(-102%);
    opacity: 1;
    z-index: 999;
  }

  .showSnake__bar__success {
    border-left: 3px solid #16fc29;
    transform: translateX(-102%);
    opacity: 1;
    z-index: 999;
  }
  .form-control:focus {
    border-color: #ced4da !important;
    box-shadow: none;
  }
  .modal {
    display: none; /* Ẩn Mặc Định */
    position: fixed;
    z-index: 1;
    padding-top: 100px; /* Vị trí của modal */
    left: 0;
    top: 0;
    width: 100%;
    height: 100%;
    overflow: auto; /* thiết lập scroll khi cần thiết */
    background-color: rgb(0, 0, 0);
    background-color: rgba(0, 0, 0, 0.4);
    min-width: 80%;
  }

  /* Nội Dung Modal */
  .modal-content {
    background-color: #fefefe;
    margin: auto;

    border: 1px solid #888;
    width: 80%;
    min-width: 80%;
    box-shadow: 0 1px 1px rgba(0, 0, 0, 0.12), 0 2px 2px rgba(0, 0, 0, 0.12),
      0 4px 4px rgba(0, 0, 0, 0.12), 0 8px 8px rgba(0, 0, 0, 0.12),
      0 16px 16px rgba(0, 0, 0, 0.12);
    border-radius: 0 !important;
  }
</style>
